def word_frequency(sentence):

    freq = {}

    for word in sentence.split():
        freq[word] = freq.get(word, 0) + 1

    return freq

print(word_frequency("python java python c java"))

Output

{'python': 2, 'java': 2, 'c': 1}
